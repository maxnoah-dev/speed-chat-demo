# Docker Compose - development (dùng với: docker compose --env-file .env.development -f docker-compose.yml -f docker-compose.dev.yml up)
APP_ENV=development
REACT_APP_API_URL=http://localhost:3002
CORS_ORIGIN=http://localhost:3003
VIRTUAL_HOST=localhost
BE_PORT=3002
