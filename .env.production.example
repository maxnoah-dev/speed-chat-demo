# Copy to .env.production on server and fill in real values.
# Docker Compose: docker compose --env-file .env.production -f docker-compose.yml up -d
APP_ENV=production
REACT_APP_API_URL=https://speed-chat.maxnoah.io.vn
CORS_ORIGIN=https://speed-chat.maxnoah.io.vn
VIRTUAL_HOST=speed-chat.maxnoah.io.vn
BE_PORT=3002
DB_HOST=mysql
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=social_demo
